import React, { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';

const Menu = ({ isRtl }) => {
  const [activeTab, setActiveTab] = useState('signature');

  const tabs = [
    { id: 'signature', name: isRtl ? 'الأطباق الرئيسية' : 'Signature Dishes' },
    { id: 'grills', name: isRtl ? 'المشويات' : 'Grills' },
    { id: 'stews', name: isRtl ? 'المرق والشوربة' : 'Stews & Soups' },
    { id: 'appetizers', name: isRtl ? 'المقبلات والسلطات' : 'Starters & Salads' },
  ];

  const menuItems = {
     signature: [
       { name: isRtl ? 'كباب كوبيده مع الأرز' : 'Kebab Kubideh with Rice', price: '4.500 KD', description: isRtl ? 'لحم مفروم مشوي متبل بالتوابل الإيرانية' : 'Groud meat kebab seasoned with Persian spices.' },
       { name: isRtl ? 'جوجه كباب' : 'Joojeh Kebab', price: '4.250 KD', description: isRtl ? 'قطع دجاج متبلة بالزعفران والليمون' : 'Saffron and lemon marinated chicken pieces.' },
       { name: isRtl ? 'كباب سلطاني' : 'Kebab Soltani', price: '8.500 KD', description: isRtl ? 'مزيج من الكباب والبرغ' : 'A combination of Kebab Barg and Kobideh.' },
       { name: isRtl ? 'كباب أبشار ممتاز' : 'Abshar Mumtaz Kebab', price: '5.750 KD', description: isRtl ? 'قطعتين من الكباب الفاخر مع الأرز' : '2 skewers of premium kebab served with saffron rice.' },
     ],
     grills: [
       { name: isRtl ? 'كباب شنف' : 'Kebab Chenjeh', price: '5.250 KD', description: isRtl ? 'قطع من لحم الغنم المشوي' : 'Grilled lamb chunks.' },
       { name: isRtl ? 'ريش غنم' : 'Lamb Chops', price: '6.500 KD', description: isRtl ? 'ريش مشوية متبلة' : 'Premium marinated ribs.' },
       { name: isRtl ? 'كباب برگ' : 'Kebab Barg', price: '6.500 KD', description: isRtl ? 'فيليه لحم بقر متبل مع الأرز' : 'Tender marinated beef fillet with rice.' },
       { name: isRtl ? 'جوجه بالعظم' : 'Joojeh with Bone', price: '4.500 KD', description: isRtl ? 'دجاج مشوي بالعظم مع الأرز' : 'Marinated chicken on the bone with rice.' },
     ],
     stews: [
       { name: isRtl ? 'قرمة سبزي' : 'Ghormeh Sabzi', price: '4.250 KD', description: isRtl ? 'مرق الأعشاب الإيراني التقليدي مع اللحم' : 'Traditional Iranian herb stew with meat.' },
       { name: isRtl ? 'فسنجان' : 'Fesenjan', price: '4.500 KD', description: isRtl ? 'دجاج بصلصة الرمان والجوز' : 'Chicken in pomegranate and walnut sauce.' },
       { name: isRtl ? 'قيمة مع الأرز' : 'Gheimeh with Rice', price: '4.250 KD', description: isRtl ? 'مرق الحمص مع اللحم والليمون' : 'Split pea stew with meat and dried lime.' },
       { name: isRtl ? 'باقالي بولو مع ساق غنم' : 'Bagheli Polo with Lamb Shank', price: '6.500 KD', description: isRtl ? 'أرز بالفاصوليا العريضة مع ساق الغنم' : 'Broad bean rice served with a tender lamb shank.' },
     ],
     appetizers: [
       { name: isRtl ? 'ميرزا قاسمي' : 'Mirza Ghasemi', price: '2.500 KD', description: isRtl ? 'باذنجان مشوي مع الثوم والطماطم' : 'Smoked eggplant with garlic and tomatoes.' },
       { name: isRtl ? 'كشك بادمجان' : 'Kashk-e-Bademjan', price: '2.500 KD', description: isRtl ? 'باذنجان مع اللبنة المجففة والنعناع' : 'Iranian eggplant dip with whey.' },
       { name: isRtl ? 'سلطة شيرازي' : 'Shirazi Salad', price: '1.500 KD', description: isRtl ? 'سلطة خيار وطماطم وبصل مقطع' : 'Diced cucumber, tomato, and onion salad.' },
       { name: isRtl ? 'ماست و خيار' : 'Mast-o Khiar', price: '1.250 KD', description: isRtl ? 'روب بالخيار والنعناع' : 'Cucumber and mint yogurt dip.' },
     ]
  };

  const categoryImages = {
    signature: 'file:///C:/Users/karti/.gemini/antigravity/brain/4644af39-7743-4186-a2b1-5da29f1e3fc1/persian_grill_tray_1775227651191.png',
    grills: 'https://images.unsplash.com/photo-1555939594-58d7cb561ad1?q=80&w=800&auto=format&fit=crop',
    stews: 'file:///C:/Users/karti/.gemini/antigravity/brain/4644af39-7743-4186-a2b1-5da29f1e3fc1/ghormeh_sabzi_stew_1775227678964.png',
    appetizers: 'https://images.unsplash.com/photo-1541529086526-db283c563270?q=80&w=800&auto=format&fit=crop'
  };

  return (
    <section id="menu" className="section-padding" style={{ background: 'var(--white)' }}>
      <div className="container">
        <div style={{ textAlign: 'center', marginBottom: '4rem' }}>
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true }}
          >
            <h2 style={{ fontSize: '3rem', marginBottom: '1rem', color: 'var(--primary)', fontFamily: 'var(--font-heading)' }}>
              {isRtl ? 'استكشف قائمتنا' : 'Explore Our Menu'}
            </h2>
            <div style={{ 
              width: '80px', 
              height: '3px', 
              background: 'var(--secondary)', 
              margin: '0 auto 1.5rem',
              borderRadius: '2px'
            }} />
            <p style={{ maxWidth: '600px', margin: '0 auto', color: 'var(--text-muted)' }}>
              {isRtl ? 'نقدم لكم أرقى تشكيلة من المأكولات الإيرانية التقليدية المحضرة بعناية وحب.' : 'Experience the finest selection of traditional Iranian cuisine, prepared with passion.'}
            </p>
          </motion.div>
        </div>

        {/* Tab Navigation */}
        <div style={{ 
          display: 'flex', 
          justifyContent: 'center', 
          gap: '1rem', 
          marginBottom: '4rem', 
          flexWrap: 'wrap'
        }}>
          {tabs.map((tab) => (
            <button
              key={tab.id}
              onClick={() => setActiveTab(tab.id)}
              style={{
                padding: '0.8rem 2rem',
                borderRadius: '50px',
                border: 'none',
                background: activeTab === tab.id ? 'var(--primary)' : 'rgba(0,0,0,0.05)',
                color: activeTab === tab.id ? 'var(--white)' : 'var(--primary)',
                fontWeight: 600,
                cursor: 'pointer',
                transition: 'all 0.3s ease',
                fontSize: '0.95rem'
              }}
            >
              {tab.name}
            </button>
          ))}
        </div>

        {/* Menu Grid */}
        <div style={{ minHeight: '600px' }}>
          <AnimatePresence mode="wait">
            <motion.div
              key={activeTab}
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              exit={{ opacity: 0, y: -20 }}
              transition={{ duration: 0.4 }}
              style={{ 
                display: 'grid', 
                gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))', 
                gap: '2.5rem' 
              }}
            >
              {menuItems[activeTab].map((item, index) => (
                <div key={index} className="menu-card" style={{
                  background: '#fff',
                  borderRadius: '1rem',
                  overflow: 'hidden',
                  boxShadow: '0 10px 30px rgba(0,0,0,0.05)',
                  transition: 'transform 0.3s ease'
                }}>
                  <div style={{ height: '200px', position: 'relative' }}>
                    <img 
                      src={categoryImages[activeTab]} 
                      alt={item.name} 
                      style={{ width: '100%', height: '100%', objectFit: 'cover' }}
                    />
                    <div style={{
                      position: 'absolute',
                      top: '1rem',
                      [isRtl ? 'left' : 'right']: '1rem',
                      background: 'var(--secondary)',
                      color: 'var(--midnight)',
                      padding: '0.4rem 1rem',
                      borderRadius: '2rem',
                      fontWeight: 'bold',
                      fontSize: '0.9rem'
                    }}>
                      {item.price}
                    </div>
                  </div>
                  <div style={{ padding: '1.5rem' }}>
                    <h3 style={{ fontSize: '1.2rem', fontFamily: 'var(--font-heading)', color: 'var(--primary)', marginBottom: '0.5rem' }}>
                      {item.name}
                    </h3>
                    <p style={{ color: 'var(--text-muted)', fontSize: '0.9rem', lineHeight: '1.6', marginBottom: '1.5rem' }}>
                      {item.description}
                    </p>
                    <button 
                      onClick={() => window.open('https://absharq8.com/', '_blank')}
                      style={{
                        width: '100%',
                        padding: '0.8rem',
                        borderRadius: '0.5rem',
                        border: '1px solid var(--secondary)',
                        background: 'transparent',
                        color: 'var(--primary)',
                        fontWeight: '600',
                        cursor: 'pointer',
                        transition: 'all 0.3s ease'
                      }}
                      onMouseOver={(e) => {
                        e.target.style.background = 'var(--secondary)';
                        e.target.style.opacity = '1';
                      }}
                      onMouseOut={(e) => {
                        e.target.style.background = 'transparent';
                      }}
                    >
                      {isRtl ? 'اطلب الآن' : 'Order Now'}
                    </button>
                  </div>
                </div>
              ))}
            </motion.div>
          </AnimatePresence>
        </div>

        <div style={{ marginTop: '5rem', textAlign: 'center' }}>
          <p style={{ marginBottom: '1.5rem', fontSize: '1.1rem', opacity: 0.7 }}>
            {isRtl ? 'هل تريد رؤية القائمة الكاملة؟' : 'Want to see the full menu?'}
          </p>
          <a 
            href="https://absharq8.com/" 
            target="_blank" 
            rel="noopener noreferrer"
            className="btn btn-primary"
            style={{ padding: '1rem 3rem', fontSize: '1.1rem', borderRadius: '50px' }}
          >
            {isRtl ? 'عرض الموقع الرسمي للطلب' : 'View Official Ordering Site'}
          </a>
        </div>
      </div>
    </section>
  );
};

export default Menu;
